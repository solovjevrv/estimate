/**
 * Цель контекстного меню правой кнопки мыши (12.9) — элемент доски или связь.
 * Перенесён из `BoardContextMenu.vue`, чтобы composable не импортировал
 * Vue-компоненты (тип живёт в feature-модуле, а .vue просто реэкспортирует его
 * для обратной совместимости).
 */
export type BoardContextMenuTarget = 'item' | 'edge';
